# =============================================================================
# Environment Configuration Template
# =============================================================================
# Copy this file to .env and update the values according to your environment.
# This file contains all environment variables used by the application.
#
# Usage:
#   cp .env.example .env
#   # Then edit .env with your actual values
# =============================================================================

# -----------------------------------------------------------------------------
# Database Configuration
# -----------------------------------------------------------------------------
# PostgreSQL database connection settings
# These values are used by both docker-compose and the backend application

# Database name
POSTGRES_DB=resume_agent

# Database user
POSTGRES_USER=postgres

# Database password (CHANGE THIS IN PRODUCTION!)
POSTGRES_PASSWORD=password

# Database host
# - Use 'postgres' when running in Docker (service name)
# - Use 'localhost' when connecting from host machine
POSTGRES_HOST=postgres

# Database port
POSTGRES_PORT=5432

# -----------------------------------------------------------------------------
# Alternative Database URL (Optional)
# -----------------------------------------------------------------------------
# If provided, this will be used instead of building URL from components above
# Format: postgresql+asyncpg://user:password@host:port/database
# DATABASE_URL=

# -----------------------------------------------------------------------------
# Application Configuration
# -----------------------------------------------------------------------------

# Debug mode
# - Set to 'true' for development (enables detailed logging, migration checks)
# - Set to 'false' for production
DEBUG=true

# =============================================================================
# Notes:
# =============================================================================
# 1. Never commit the actual .env file to version control
# 2. Use strong passwords in production environments
# 3. For local development, the default values should work with docker-compose
# 4. POSTGRES_HOST should match the service name in docker-compose.yml when
#    running in Docker, or 'localhost' when connecting from outside Docker
# =============================================================================
